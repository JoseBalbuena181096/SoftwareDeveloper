// imports

// code(objects, functions operations)
function sumar(a: number, b: number): number {
  return a + b;
}

function saludar(name: string): string {
  return "Hello " + name;
}

// exports

export { sumar, saludar };
